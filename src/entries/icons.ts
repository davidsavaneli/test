// Public surface for `sava-test/icons` — the Icon component, its `IconName` union, the name list,
// and the raw SVG registry (e.g. for building an icon picker).
export * from '../components/Icon'
export { icons } from '../icons/icons'
